<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/map_nav_graph"
    app:startDestination="@id/map_fragment">

    <fragment
        android:id="@+id/map_fragment"
        android:name="com.chrsrck.quakemap.ui.EarthquakeMapFragment"
        android:label="map_fragment">

        <action
            android:id="@+id/action_map_fragment_to_earthquake_list_fragment"
            app:destination="@id/earthquake_list_fragment"
            />

        <action
            android:id="@+id/action_map_fragment_to_earthquake_settings_fragment"
            app:destination="@id/settings_fragment"
            />
    </fragment>

</navigation>