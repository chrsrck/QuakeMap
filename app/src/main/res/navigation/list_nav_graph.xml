<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_nav_graph"
    app:startDestination="@id/earthquake_list_fragment"
    >

    <fragment
        android:id="@+id/earthquake_list_fragment"
        android:name="com.chrsrck.quakemap.ui.EarthquakeListFragment"
        android:label="earthquake_list_fragment"
        tools:layout="@layout/fragment_earthquake_list">

        <action
            android:id="@+id/action_earthquake_list_fragment_to_map_fragment"
            app:destination="@id/map_fragment"
            />

        <action
            android:id="@+id/action_earthquake_list_fragment_to_settings_fragment"
            app:destination="@id/settings_fragment"
            />
    </fragment>
</navigation>