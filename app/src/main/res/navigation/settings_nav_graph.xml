<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/settings_nav_graph"
    app:startDestination="@id/settings_fragment">

    <fragment
        android:id="@+id/settings_fragment"
        android:name="com.chrsrck.quakemap.ui.SettingsFragment"
        android:label="settings_fragment"
        tools:layout="@xml/preferences_settings"
        >

        <action
            android:id="@+id/action_settings_fragment_to_map_fragment"
            app:destination="@id/map_fragment"
            />

        <action
            android:id="@+id/action_settings_fragment_to_earthquake_list_fragment"
            app:destination="@id/earthquake_list_fragment"
            />
    </fragment>

</navigation>